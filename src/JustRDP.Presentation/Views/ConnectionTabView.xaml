<UserControl x:Class="JustRDP.Presentation.Views.ConnectionTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:JustRDP.Presentation.ViewModels"
             xmlns:converters="clr-namespace:JustRDP.Presentation.Converters"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:ConnectionTabViewModel}"
             Loaded="ConnectionTabView_Loaded"
             Unloaded="ConnectionTabView_Unloaded">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid>
        <!-- RDP Host -->
        <WindowsFormsHost x:Name="WinFormsHost"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch" />

        <!-- Connecting Overlay -->
        <Border Background="#80000000"
                Visibility="{Binding IsConnecting, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                             IsIndeterminate="True" Width="40" Height="40" />
                <TextBlock Text="{Binding StatusMessage}" Foreground="White"
                           HorizontalAlignment="Center" Margin="0,12,0,0" FontSize="14" />
            </StackPanel>
        </Border>

        <!-- Error Overlay -->
        <Border Background="#80000000"
                Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" MaxWidth="400">
                <materialDesign:PackIcon Kind="AlertCircle" Width="48" Height="48"
                                         HorizontalAlignment="Center" Foreground="OrangeRed" />
                <TextBlock Text="{Binding ErrorMessage}" Foreground="White" TextWrapping="Wrap"
                           HorizontalAlignment="Center" Margin="0,12,0,16" FontSize="14"
                           TextAlignment="Center" />
                <Button Content="Reconnect" Command="{Binding ReconnectCommand}"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Foreground="White" BorderBrush="White"
                        HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
